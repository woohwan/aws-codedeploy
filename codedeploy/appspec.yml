version: 0.0
os: linux
files:
  - source: /
    destination: /opt/tomcat/webapps
permissions:
  - object: /opt/tomcat/webapps
    pattern: "*"
    owner: root
    mode: 777
    type:
      - file

hooks:
  AfterInstall:
    - location: scripts/after_deploy.sh
      timeout: 300
      runas: root
  